#!/bin/bash
# AGI IN ACTION Launcher with Terminal Logs
# Double-click this file to launch the app with visible logs

cd "$(dirname "$0")"
source venv/bin/activate
python "AGI IN ACTION(BASIC) 2.3.py"
